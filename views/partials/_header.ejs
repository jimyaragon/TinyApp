<header>
    <nav class="navbar navbar-expand-md navbar-dark bg-success">
      <a class="navbar-brand" href="/urls">TinyApp</a>
      <button
        class="navbar-toggler"
        type="button"
        data-toggle="collapse"
        data-target="#navbarNavAltMarkup"
        aria-controls="navbarNavAltMarkup"
        aria-expanded="false"
        aria-label="Toggle navigation"
      >
        <span class="navbar-toggler-icon"></span>
      </button>
      
      <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav">
          <a class="nav-item nav-link" href="/urls">My URLs</a>
          <a class="nav-item nav-link" href="/urls/new">Create New URL</a>
        </div>
      </div>
  
  
      <% if (user) { %>
        <h4 class="navbar-text">
          <span class="navbar-text ml-3">Welcome, <%= user.email %></span>
        </h4>
        <form action="/logout" method="POST" class="ml-3">
          <button type="submit" class="btn btn-danger">Log Out</button>
        </form>
      <% } else { %>
       
           <div class="navbar-nav"></div>
            <button type="button" class="btn btn-light mr-3 btn-sm btn-link"><a  href="/urls_register">Register</a></button>
            <button type="button" class="btn btn-light btn-sm"><a href="/urls_login">Login</a></button>
         </div>
  
        <% } %>
  
        </nav>
  </header>